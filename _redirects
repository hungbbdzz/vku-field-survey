# Cloudflare Pages configuration
[build]
  command = "npm run build"
  destination = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback — redirect all routes to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
